/**
 * Utility Functions Module
 * Provides common utility functions for logging and general helper tasks
 */

/**
 * Logs a message to the console
 * @param msg The message string to log
 */
export function log(msg: string): void {
  console.log(msg);
}
